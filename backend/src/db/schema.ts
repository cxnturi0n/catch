// Drizzle schema — single source of truth for the database.
// Populated in step 2 (auth tables) and step 3 (application tables ported
// from legacy/supabase/migrations). Kept empty on purpose in step 1 so the
// skeleton boots against a fresh database.
export {}
